---
title: <Drawer>
---

import { EditorPreview } from "@/docs/components/Preview";
import { Editor, Drawer } from "@/editor";

# \<Drawer\>

A list of items that can be dragged into a [`<Editor.Preview>`](editor-preview). Used for composing custom Frontend UIs.

<EditorPreview
  config={{ components: { Orange: { render: () => <div>Orange</div> } } }}
  data={{ root: { props: {} }, content: [] }}
>
  <Drawer>
    <Drawer.Item name="Orange" />
  </Drawer>
</EditorPreview>

```tsx {6-8} /Drawer/1 copy
import { Editor, Drawer } from "@frontendai/react-editor";

export function Editor() {
  return (
    <Editor>
      <Drawer>
        <Drawer.Item name="Orange" />
      </Drawer>
    </Frontend>
  );
}
```

## Props

| Param                   | Example                     | Type      | Status   |
| ----------------------- | --------------------------- | --------- | -------- |
| [`children`](#children) | `children: <Drawer.Item />` | ReactNode | Required |

## Required props

### `children`

A React node representing the contents of the `<Drawer>`. Will likely contain [`<Drawer.Item>`](drawer-item) nodes.
