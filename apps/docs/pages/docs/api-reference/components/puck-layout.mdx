---
title: <Puck.Layout>
---

import { PuckPreview } from "@/docs/components/Preview";
import { Puck } from "@/editor";

# \<Puck.Layout\>

Render the standard Puck layout. This is useful for implementing behavior using the [internal Puck API](/docs/extending-editor/internal-editor-api) without changing the default UI.

```tsx {13} showLineNumbers copy
import { Puck } from "@editoreditor/core";

const Example = ({ children }) => {
  useEffect(() => console.log("Hello, world"), []);

  return <>{children}</>;
};

export function Editor() {
  return (
    <Puck>
      <Example>
        <Puck.Layout />
      </Example>
    </Puck>
  );
}
```

## Props

This component doesn't accept any props.
